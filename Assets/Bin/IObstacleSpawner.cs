/*using UnityEngine;

public enum SpawnDirection { Left, Right }

public interface IObstacleSpawner
{
    // Activate the spawner to repeatedly spawn obstacles at the given interval and in the specified direction.
    void ActivateSpawner(float interval, SpawnDirection direction);

    // Deactivate the spawner, stopping the continuous spawn process.
    void DeactivateSpawner();
}*/